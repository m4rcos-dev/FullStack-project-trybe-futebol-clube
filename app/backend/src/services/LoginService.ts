// import sequelize from '../database/models';
// import UserModel from '../database/models/UserModel';

// export default class LoginService {
//   public async sigInUser(email: string) {
//     const user = await sequelize.models.UserModel.findOne({ where: { email } });
//     if (!user) return { status: 400, message: 'Invalid' };
//   }
// }
